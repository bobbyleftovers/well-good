(function ( $ ) {
  module.exports = function( el ) {
    /*this.$el = $( el );
    $('body').on('inview', '[data-inview-class]', function(event, visible) {
        var inview_class = $(this).attr('data-inview-class');
        console.log('inview');
        console.log($(this));
        if (visible == true && !$(this).hasClass(inview_class)) {
          $(this).addClass(inview_class);
          $(this).unbind('inview');
          // $(window).trigger('resize');
        }
    });
    $( window ).trigger('scroll');*/
  };
})( jQuery );